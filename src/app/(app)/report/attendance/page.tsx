import * as React from 'react';

type Props = {
    children?: React.ReactNode
    
};

export default function Attendance(props: Props) {
    return (
        <main>
        
        </main>
    );
};