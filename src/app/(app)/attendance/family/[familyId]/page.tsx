import * as React from 'react';

type Props = {
    children: React.ReactNode
    
};

export default function AttendanceFamilyId(props: Props) {
    return (
        <main>
            {
                /**
                 * TODO: 
                 */
            }
        </main>
    );
};